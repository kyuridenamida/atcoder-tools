#include <bits/stdc++.h>
using namespace std;

void solve(long long N, vector<long long> A){

}

int main(){
    long long N;
    scanf("%lld",&N);
    vector<long long> A(N-1+1);
    for(int i = 0 ; i <= N-1 ; i++){
        scanf("%lld",&A[i]);
    }
    solve(N, A);
    return 0;
}

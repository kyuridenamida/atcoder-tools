#include <bits/stdc++.h>
using namespace std;

void solve(long long N, vector<long long> A){

}

int main(){
    long long N;
    scanf("%lld",&N);
    vector<long long> A(N);
    for(int i = 0 ; i < N ; i++){
        scanf("%lld",&A[i]);
    }
    solve(N, A);
    return 0;
}
